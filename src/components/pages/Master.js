import React from 'react';
import Tabs from '@material-ui/core/Tabs';
import Tab from '@material-ui/core/Tab';

class Master extends React.Component {
    render() {
        let page = <div>Master</div>;

        return(page);
    }
}

export default Master;